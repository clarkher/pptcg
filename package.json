{
  "name": "pptcg",
  "version": "1.0.0",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd backend && npm ci && ./node_modules/.bin/prisma generate && npm run build",
    "start": "cd backend && node dist/index.js",
    "backend": "cd backend && npm run dev"
  }
}
